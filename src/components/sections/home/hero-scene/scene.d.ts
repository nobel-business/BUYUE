/** Vendored WebGL hero (scene.js). Boots the Three.js scene into `mount` and returns
 *  a teardown function (stops loops, drops listeners/observer, frees the GL context). */
export function initHeroScene(mount: HTMLElement): () => void;
